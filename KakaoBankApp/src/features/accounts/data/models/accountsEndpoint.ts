/** Firestore path: secrets/endpoint → field `accounts` (API base URL). */
export const ACCOUNTS_ENDPOINT_FIRESTORE = {
  collection: 'secrets',
  document: 'endpoint',
  field: 'accounts',
} as const;
